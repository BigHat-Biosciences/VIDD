# Antibody (task='ab') extras for VIDD. Install on top of the base requirements:
#   pip install -r requirements.txt
#   pip install -r requirements_ab.txt
#
# Additionally, NanoBodyBuilder2 needs pdbfixer + openmm via conda, and ANARCI
# needs HMMER. Suggested install:
#   conda install -c conda-forge pdbfixer openmm
#   conda install -c bioconda hmmer
#
# AF2 weights: download into $AF_PARAMS_DIR (default ~/.mber/af_params) per
#   mber-open/download_weights.sh.
# NBB2 weights: will be downloaded on first NanoBodyBuilder2() construction
#   into $NBB2_WEIGHTS_DIR (default ~/.mber/nbb2_weights).

# AF2 backend used by evaluations/ab_af2_reward.py.
colabdesign @ git+https://github.com/sokrypton/ColabDesign.git@d024c4e

# JAX must be pinned here (not just upgraded later) so pip's resolver picks
# chex / optax / numpy versions compatible with jax 0.5.2 on the first pass.
# Without this pin, pip installs jax 0.10.0 + chex>=0.1.90 + optax>=0.2.8 +
# numpy>=2, then install.sh's GPU upgrade downgrades jax and strands the rest.
# Default wheel is CPU jaxlib; install.sh layers `jax[cuda12]==0.5.2` on top
# when CUDA != cpu. Forward-compatible with CUDA 12+ drivers (incl. CUDA 13).
jax==0.5.2
flax<0.10.0

# NanoBodyBuilder2 (provides one-shot binder template via NBB2).
ImmuneBuilder
